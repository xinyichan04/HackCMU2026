> **ARCHIVED (2026-09-12).** The project consolidated on the browser 3D head
> (`poc/web/live3d.html`); everything in `poc/archive/` is the earlier 2D/photo
> python work, kept runnable and un-archivable in one `git mv`. Paths in the
> commands below are relative to the old layout — prepend `archive/` where a
> `poc/...py` path appears.

