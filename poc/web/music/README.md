# music/

Drop LE SSERAFIM mp3s (or .m4a/.ogg/.wav) in here — the LE SSERAFIMIFY page
finds them through the dev server's directory listing and plays them as the
background soundtrack (shuffled).

Audio files are gitignored on purpose: copyrighted tracks stay on your own
machine and never enter the repo.
